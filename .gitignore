node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.DS_Store
logs/
# Data files are generated by CI (GitHub Actions) and committed back automatically.
# Exclude from local dev commits only — CI uses `git add -f` to force-add them.
public/data/active-alerts.json
public/data/zones-lookup.json
